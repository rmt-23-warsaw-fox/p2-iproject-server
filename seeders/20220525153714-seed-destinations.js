'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    /**
     * Add seed commands here.
     *
     * Example:
     * await queryInterface.bulkInsert('People', [{
     *   name: 'John Doe',
     *   isBetaMember: false
     * }], {});
    */

    const destinations = require('../destination.json')

    destinations.forEach(destination => {
      destination.createdAt = new Date()
      destination.updatedAt = new Date()
    })

    await queryInterface.bulkInsert('Destinations', destinations, {})
  },

  async down(queryInterface, Sequelize) {
    /**
     * Add commands to revert seed here.
     *
     * Example:
     * await queryInterface.bulkDelete('People', null, {});
     */
    await queryInterface.bulkInsert('Destinations', null, {})
  }
};
